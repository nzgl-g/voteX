const express = require("express");
const router = express.Router();
router.get("/", async (req, res) => {});
module.exports = router;
// do u need this route for block chain linking? probably idk . if yes tell me
